"""프로젝트 설정. 종목이나 지표를 바꾸고 싶으면 이 파일만 고치면 된다."""

# ── 종목 ────────────────────────────────────────────────────────
# code: 한국거래소 6자리 종목코드
# peers: 이 종목과 같이 움직이는 미국 종목 (야간 흐름 참고용)
# keywords: 뉴스 검색에 쓸 말. 회사 이름만으로는 놓치는 기사가 많다.
# ticker: 야후 파이낸스 조회용. 코스피는 .KS, 코스닥은 .KQ 를 붙인다.
STOCKS = {
    "000660": {
        "name": "SK하이닉스",
        "ticker": "000660.KS",
        "peers": ["MU", "NVDA"],
        "keywords": ["SK하이닉스", "HBM", "메모리 반도체"],
    },
    "005930": {
        "name": "삼성전자",
        "ticker": "005930.KS",
        "peers": ["MU", "TSM"],
        "keywords": ["삼성전자", "삼성 반도체", "파운드리"],
    },
}

# ── 구독자 ──────────────────────────────────────────────────────
# 사람마다 받을 종목을 정한다. 2단계에서 가족을 추가한다.
SUBSCRIBERS = [
    {"name": "SH.YI", "stocks": ["000660", "005930"]},
]

# ── 밤새 지표 ───────────────────────────────────────────────────
# 카드 2번(밤새 지표)에 들어갈 값들. 야후 파이낸스 티커 기준.
MARKET_INDICATORS = [
    ("^GSPC", "S&P 500", "pct"),
    ("^IXIC", "나스닥", "pct"),
    ("^SOX", "SOX 반도체", "pct"),
    ("CL=F", "WTI 유가", "price_pct"),
    ("^TNX", "미 10년 금리", "rate"),
    ("KRW=X", "원/달러", "price_pct"),
    ("^VIX", "VIX 공포지수", "level_pct"),
    ("GC=F", "금", "pct"),
]

# ── 거시 뉴스 검색어 ────────────────────────────────────────────
# 개별 종목과 무관하게 시장 전체를 흔드는 이슈를 찾는다.
MACRO_KEYWORDS = [
    "연준 금리",
    "국제유가",
    "환율 원달러",
    "미국 CPI 물가",
    "반도체 수출규제",
    "중동 정세 유가",
]

# ── 발송 ────────────────────────────────────────────────────────
SEND_HOUR = 7  # KST

# 카드뉴스가 올라갈 주소. GitHub Pages 를 켜면 그 주소를 여기에 적는다.
#   예: "https://ysh971007.github.io/morning-report"
# 비어 있으면 카톡에 글자만 보낸다.
# 주소를 적기 전에 그 도메인을 카카오 콘솔의
# [앱 > 제품 링크 관리 > 웹 도메인] 에 등록해야 버튼이 동작한다.
SITE_URL = ""

# ── 카드 문장을 만들 모델 ───────────────────────────────────────
# "gemini" 는 무료 한도 안에서 쓸 수 있고, "claude" 는 크레딧을 충전해서 쓴다.
# 이 값만 바꾸면 나머지 코드는 그대로 돌아간다.
PROVIDER = "gemini"

# aistudio.google.com/apikey 에서 무료 키를 받아 .env 의 GEMINI_API_KEY 에 넣는다.
# 한도에 걸리면 gemini-3.5-flash-lite 처럼 더 가벼운 모델로 낮추면 된다.
GEMINI_MODEL = "gemini-3.6-flash"

# 기본 모델이 계속 붐비거나 한도에 걸릴 때 대신 부를 모델.
# 무료 한도는 모델마다 따로 계산되므로 예비 모델을 다른 계열로 두는 게 낫다.
GEMINI_FALLBACK_MODEL = "gemini-3.5-flash-lite"

# console.anthropic.com 에서 키를 받아 .env 의 ANTHROPIC_API_KEY 에 넣는다.
CLAUDE_MODEL = "claude-opus-5"
CLAUDE_EFFORT = "medium"

# 카드에 넣을 최대 개수
MAX_MACRO_ISSUES = 2
MAX_STOCK_NEWS = 2

# ── 사진 ────────────────────────────────────────────────────────
# 카드 배경 사진을 매일 주제에 맞게 새로 받아오려면 Pixabay 무료 키가 필요하다.
# pixabay.com 가입 후 pixabay.com/api/docs 에서 즉시 발급되며,
# 받은 키는 .env 에 PIXABAY_API_KEY= 로 넣는다.
# 키가 없으면 assets/ 의 고정 사진을 쓴다.

# 위키미디어를 예비 경로로 쓸지. 기본은 끔.
# 자동 검색 품질이 나빠서(그래프·엉뚱한 사진) 실사용에는 권하지 않는다.
USE_WIKIMEDIA_FALLBACK = False
